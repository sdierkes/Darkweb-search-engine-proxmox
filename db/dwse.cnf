# mariadb.cnf
[mysqld]
skip_name_resolve=ON
max_allowed_packet=64M
net_read_timeout=120
net_write_timeout=120
wait_timeout=28800
interactive_timeout=28800
# log_error=/var/log/mysql/mariadb_error_log
# log_warnings=9
# general_log = on
# general_log_file=/var/log/mysql/mariadb.log
